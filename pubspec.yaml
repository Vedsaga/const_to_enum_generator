name: const_to_enum_generator
description: A Dart code generator an enum for static const strings in a class.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  analyzer: ^8.4.1
  build: ^3.0.0
  source_gen: ^4.0.0
  const_to_enum_annotations:
    git:
      url: https://github.com/Vedsaga/const_to_enum_annotations.git
      ref: main

dev_dependencies:
  build_test: ^3.3.3
  mocktail: ^1.0.0
  test: ^1.25.0
  very_good_analysis: ^7.0.0